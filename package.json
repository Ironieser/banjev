{
  "name": "banjev",
  "version": "0.0.2",
  "private": true,
  "description": "节约读论文时间，节约读论文的 token / Save time and tokens reading papers",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "node tests/e2e.mjs",
    "update-data": "node scripts/update-data.mjs",
    "package": "rm -f banjev.zip && zip -r banjev.zip manifest.json icons src data -x '*.DS_Store'",
    "build-site": "sh scripts/build-site.sh",
    "screenshot": "node scripts/screenshot.mjs"
  },
  "devDependencies": {
    "puppeteer": "^25.12.0"
  },
  "license": "Apache-2.0"
}
